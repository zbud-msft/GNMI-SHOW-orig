trigger:
  branches:
    include:
    - master

pr:
  branches:
    include:
    - master

stages:
- stage: Build
  jobs:
  - job: BuildAndTest
    displayName: "Install & Test CLI"
    timeoutInMinutes: 15

    pool:
      vmImage: ubuntu-22.04

    steps:
    - checkout: self
      clean: true
      submodules: recursive
      displayName: 'Checkout code (with submodules)'

    - task: UsePythonVersion@0
      inputs:
        versionSpec: '3.10'
      displayName: 'Use Python 3.10'

    - script: |
        set -euo pipefail
        make install
        .venv/bin/pip install pytest
      displayName: 'Install (venv + symlink) and test deps'

    - script: |
        set -euo pipefail
        mkdir -p test-results
        PYTHONPATH=.:GNMI-CLI-Converter/python .venv/bin/python -m pytest tests/ -v --junit-xml=test-results/results.xml
      displayName: 'Run tests'

    - task: PublishTestResults@2
      displayName: 'Publish test results'
      condition: always()
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: 'test-results/results.xml'
        failTaskOnFailedTests: true
        testRunTitle: 'show_cli tests'
